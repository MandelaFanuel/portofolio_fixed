# Portfolio Mandela KASUMBA Fanuel

Frontend statique React + TypeScript + Vite, deployable sur Vercel.

## Lancer en local

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
npm run preview
```

Vercel utilise `npm run build` et publie le dossier `dist`.
